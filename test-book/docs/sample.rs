/// A sample struct for demonstration purposes.
pub struct Sample {
    pub id: u32,
    pub name: String,
}

/// A sample enum for demonstration purposes.
pub enum Status {
    Active,
    Inactive,
    Pending,
}
